# REST API

```
GET /users → список всех пользователей
GET /users/:id → один пользователь
POST /users → создать
PUT /users/:id → обновить
DELETE /users/:id → удалить
GET /users/:id/chats → Чаты пользователя
```