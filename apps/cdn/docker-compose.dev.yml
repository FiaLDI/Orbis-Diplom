services:
  orbis-cdn-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4005:4005"
    environment:
      - CHOKIDAR_USEPOLLING=true
    env_file:
      - ../../.env
      
