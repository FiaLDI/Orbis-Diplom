export interface Props {
  projectId: string;
  serverId: string;
  projectName: string;
  projectDescription: string;
}

export interface ProjectEditFormData {
  name: string;
  description: string;
}
